# 260908-site-v1 用户验证

## 验证说明

- 验证对象："What I've Learned" V1 全站——时间线首页、Atlas 知识星图、待学清单、材料详情页、分类/标签页、⌘K 命令面板、OG 分享卡
- 环境/前置条件：
  - 本地预览：`pnpm build && pnpm preview --port 4322` → http://localhost:4322/i-ve-learned
  - 当前站点内容为 31 条 demo 条目（带"示例"角标），用于展示形态；真实内容回填见验证项 7
  - 自动化门禁（lint / 43 单元测试 / 构建 / 7 条 e2e / 6 项体积预算）已全部通过，无需重复验证；下表只列**需要人眼/真人操作**的核心场景

## 验证项

| 验证步骤 | 预期结果 | 实际结果 | 状态 | 备注/证据 |
| --- | --- | --- | --- | --- |
| 1. 打开首页，从上往下滚动 | 站点自述一句话 → 数据行（22 learned · 44h…）→ 12 个月热力带（hover 日期有提示）→ 筛选 chips 渐显 → 年份分组时间线；滚动时条目轻柔进场 | | 待验证 | |
| 2. 点击筛选 chips：`paper`，再点 `Systems` | 列表实时过滤，摘要显示 "4 / 22 items"；空月份自动收起；再点已选 chip 或 all 恢复 | | 待验证 | |
| 3. 进入 Atlas（导航栏 Atlas），等入场动画 | 墨点星座在纸面上散开；字段靶心 + 子分类菱形 + 材料圆点；滚轮缩放、拖拽平移、hover 时非邻居变淡；点节点出侧栏（材料→view note；分类→browse field） | | 待验证 | |
| 4. Atlas 勾选 "learned only" 再取消；关掉 cross-links | 待学圆点（空心）隐藏/恢复；跨分类虚弧消失/恢复 | | 待验证 | |
| 5. 从时间线点开任意条目详情页 | 衬线笔记正文排版舒适（中文行高）；代码块在纸面上；右侧 rating/关联条目；窄屏时侧栏折到正文下方 | | 待验证 | |
| 6. 任意页面按 ⌘K（或 Ctrl+K），输入 `raft`，再输入中文如 `深入浅出` | 面板弹出无卡顿；英文走前缀/模糊搜索，中文走标题子串匹配；↑↓ 选择、Enter 跳转、Esc 关闭 | | 待验证 | |
| 7. 用真实学习史替换 demo：`grep -l 'demo: true' content/learned/*.md content/backlog/*.md \| xargs rm`，再按 `content/*/_template.md` 写入真实条目，`pnpm build` | 构建通过；全站数字/星图/热力带反映真实数据；URL 结构不变 | | 待验证 | demo 移除前站点不应对外发布 |
| 8. 手机（或 DevTools 375px 宽）走完：首页 → 详情 → Atlas → Backlog | 无横向滚动；星图自动适配窄屏（标签不被裁切）；Backlog 统计行不拆词；一切可读可点 | | 待验证 | |
| 9. 部署：建 GitHub 仓库并 push；把 `astro.config.mjs` 的 `site` 改为 `https://<用户名>.github.io/<仓库名>`；仓库 Settings → Pages → Source 选 "GitHub Actions" | push 后 Actions 自动构建并通过预算门禁；站点在 Pages URL 上线；分享链接到 IM 出现纸感 OG 卡片 | | 待验证 | deploy.yml 已就绪 |

## 验证结论

待验证。

## 待跟进

- `astro.config.mjs` 的 `site` 与页脚 "source" 链接需在部署时替换为真实仓库地址（当前为占位符）
- V1.1 候选（review.md §13）：Firefox 下星图字段标签字距兼容性验证、子分类间距余量评估、Pagefind 全文搜索
